import { Box, styled } from '@mui/material'

export const ServiceSectionWrapper = styled(Box)(({ theme }) => ({
  backgroundColor: theme.palette.background.default,
  textAlign: 'center',
  paddingTop: theme.spacing(6),
}))
